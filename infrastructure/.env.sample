# User and group to use inside the container (align with your host user to avoid root-owned files)
BUILDER_UID=$(id -u)
BUILDER_GID=$(id -u)
BUILDER_USER=builder
BUILDER_GROUP=builder

# Host directories to mount inside the container
WORK_DIR_HOST=../

# Timezone inside the container
TZ=UTC
